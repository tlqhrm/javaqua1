<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.prg//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.QnaMapper">
	<insert id="write">
		insert into qna(qna_num, pd_num, user_id, title, content)
	 	values(QNA_SEQ.NEXTVAL,#{pd_num},#{user_id}, #{title}, #{content})
	</insert>
	
	<select id="qna_list" resultType="org.zerock.domain.QnaVO">	
		<![CDATA[ SELECT * FROM ( 
	        SELECT A.*, 
	        ROWNUM AS RNUM FROM (
	            SELECT * FROM qna WHERE pd_num=#{pd_num} order by writedate desc
	        ) A
        ) WHERE RNUM >= #{startNum} AND RNUM <= #{endNum} ]]>
	</select>

	<select id="qna_cnt" resultType="int">
		select count(*) from qna where pd_num=#{pd_num}
	</select>
</mapper>